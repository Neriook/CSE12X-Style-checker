# CSE12X-Style-checker
An automatic style checker for Code quality guide from CSE intro classes.  
It supports .java files.
If the code has incorrect syntax, or does not work, the processer cannot check it.
